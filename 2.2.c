#include<stdio.h>
int main()
{
	char c;
	scanf("%c",&c);
	printf("the character is %c",c);
	printf("the character is %d",c);
		printf("the character is %o",c);
			printf("the character is %x",c);
	return 0;
}
